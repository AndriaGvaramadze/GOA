let total_coins = parseInt(prompt("Enter the total number of coins you have:"));
let spent_coins = parseInt(prompt("Enter the number of coins you spent:"));

let remaining_coins = total_coins - spent_coins;

alert("Remaining coins: " + remaining_coins);
